{
  "name": "brad-json-server",
  "version": "1.0.0",
  "description": "deploy json server to vercel",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "json-server": "^0.17.0"
  }
}
